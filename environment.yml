name: recruit
channels:
  - conda-forge
dependencies:
  - compilers
  - cxx-compiler
  - python
  - cppcheck
  - clang-tools
  - cudatoolkit-dev
  - pre-commit
  - yamllint
  - cmake
  # Documentation
  - doxygen
  # Install Python dependencies only available on PyPI
  - pip
  - pip:
      - clang-format
      - cmakelang
      - flawfinder
