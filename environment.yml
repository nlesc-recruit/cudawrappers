name: recruit
channels:
  - conda-forge
dependencies:
  - c-compiler
  - cxx-compiler
  - clang
  - python
  - cppcheck
  - clang-tools
  - cudatoolkit-dev
  - pre-commit
  - yamllint
  - cmake
  # Documentation
  - doxygen
  # Install Python dependencies only available on PyPI
  - pip
  - pip:
      - clang-format
      - cmakelang
      - flawfinder
